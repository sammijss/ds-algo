#include "BST.hpp"

int main()
{
    Node* root = NULL;
    root = AddNode(root, 50);
    AddNode(root, 30);
    AddNode(root, 20);
    AddNode(root, 40);
    AddNode(root, 70);
    AddNode(root, 60);
    AddNode(root, 80);

    return(0);
}
